# Current Task — Rename disputes.reason → description

Rename the disputes `reason` column to `description` and update backend/frontend references (disputes only).

## Acceptance Criteria

- [x] Migration renames column.
- [x] Seed/repos/API/UI use `description` for disputes.
