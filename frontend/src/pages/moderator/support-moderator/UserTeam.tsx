import UserTeamPage from "@/pages/admin/userTeam/UserTeamPage";

/** Same User & Team desk as Admin — overview, team accounts, platform users. */
export default function SupportUserTeam() {
  return <UserTeamPage variant="support" />;
}
